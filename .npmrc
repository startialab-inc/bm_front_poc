# pnpm 設定
# node_modules/.bin を含む公開パッケージのみ hoist し、ファントム依存を排除する
shamefully-hoist=false
public-hoist-pattern[]=*

# プロジェクト固有の Node.js バージョン固定（グローバル設定に影響しない）
# pnpm がスクリプト実行時に使う Node.js バージョンを強制する
use-node-version=24.15.0

# Storybook が動作しない場合は以下を有効にする
# shamefully-hoist=true
